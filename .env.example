# Example environment variables for deep-research
# Copy this file to .env and fill in your values

# Required: OpenAI API key (used for agent / OpenAI SDK features)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Port for local/dev (Gradio binds to this)
PORT=7860  # Optional, default: 7860

# Optional: SendGrid (email delivery)
# If not provided, email features will be skipped or use defaults
SENDGRID_API_KEY=your_sendgrid_api_key_here  # Optional
FROM_EMAIL=noreply@example.com  # Optional, default: noreply@example.com
TO_EMAIL=recipient@example.com  # Optional, default: recipient@example.com

# Optional: Other email/SMTP settings
# SMTP_SERVER=127.0.0.1            # For local MailHog use 127.0.0.1 (compose exposes 1025)
# SMTP_PORT=1025                   # MailHog default SMTP port
# SMTP_USERNAME=                   # Usually empty for MailHog
# SMTP_PASSWORD=
# SMTP_STARTTLS=0                  # Set to 1 for servers requiring STARTTLS

# MailHog UI available at http://localhost:8025 when using docker compose
